# Shared skills

This directory holds lightweight skills that are safe to share across all superprojects.

The sync hub exposes this directory as the shared skill catalog. Clients copy these skills into the local Codex skill area during alignment.

